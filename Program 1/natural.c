#include<stdio.h>

int main(){
    int n;
    n = 10;
    // printf("write number to display:");
    // scanf("%d",&n);
    for (int i = 1 ; i <= n; i++)
    {
        printf("%d\n",i);
    }
    return 0;
}